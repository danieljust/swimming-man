import Ember from 'ember';
//import ModelValidatorMixin from 'swimming-frontend/mixins/model-validator';
import { module, test } from 'qunit';

module('Unit | Mixin | model validator');

// Replace this with your real tests.
// test('it works', function(assert) {
//   let ModelValidatorObject = Ember.Object.extend(ModelValidatorMixin);
//   let subject = ModelValidatorObject.create();
//   assert.ok(subject);
// });
