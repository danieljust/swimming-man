{{bs-datetimepicker date=changeset.date
                    format='YYYY-MM-DD HH:mm'
                    maxDate=currentDate
                    showClose=true
                    useCurrent=true
                    todayHighlight=true
                    updateDate=(action (mut changeset.date))}}
