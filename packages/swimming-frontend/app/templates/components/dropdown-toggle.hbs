<span>{{lable}}&nbsp;</span>
<div class="btn-group">
  <button type="button" class="btn btn-default dropdown-toggle" data-toggle="dropdown">
      {{currentItem.name}}
   <span class="caret"></span>
  </button>

  <ul class="dropdown-menu" role="menu">
    {{#each items as |item|}}
      <li {{action 'checkItem' item}}>
        <a href="#">
          {{item.name}}
        </a>
      </li>
    {{/each}}
  </ul>

</div>