<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#main-navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to 'index' class="navbar-brand"}}Swimming man{{/link-to}}
    </div>

    <div class="collapse navbar-collapse" id="main-navbar">
      <ul class="nav navbar-nav">
        {{#link-to 'index' tagName="li"}}<a href="">Home</a>{{/link-to}}
        {{#if session.isAuthenticated}}
          {{#link-to 'trainings.index' (query-params userId=user.userId) tagName="li"}}<a href="">Trainings</a>{{/link-to}}
          {{#if user.canViewUsers}}
            {{#link-to 'users' tagName="li"}}<a href="">Users</a>{{/link-to}}
          {{/if}}
        {{else}}
          {{#link-to 'registration' tagName="li"}}<a href="">Registration</a>{{/link-to}}
          {{#link-to 'login' tagName="li"}}<a href="">Login</a>{{/link-to}}
        {{/if}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if session.isAuthenticated}}
          {{#link-to 'users.edit' user.userId tagName="li"}}<a href="">{{user.username}}</a>{{/link-to}}
          <li><button class="btn btn-default btn-logout" {{action logout}}>Logout</button></li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>
